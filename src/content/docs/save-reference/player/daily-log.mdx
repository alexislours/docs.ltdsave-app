---
title: DailyLog
description: 'Reference for the DailyLog fields in Player.sav of Tomodachi Life: Living the Dream.'
sidebar:
  label: DailyLog
schema: player
path: DailyLog
kind: group
leafCount: 46
leaves:
  - name: AiCountByFocusLeading
    hash: '0x6d445df7'
    type: UInt
    state: documented
  - name: AiCountByPickUp
    hash: '0x0bf422a5'
    type: UInt
    state: documented
  - name: BuyMarketMorning
    hash: '0x995f5fbf'
    type: UInt
    state: documented
  - name: BuyMarketNight
    hash: '0x8554266f'
    type: UInt
    state: documented
  - name: BuyMarketNoon
    hash: '0x5c3c8077'
    type: UInt
    state: documented
  - name: BuyMarketSale
    hash: '0x4cca73c9'
    type: UInt
    state: documented
  - name: CoinGold
    hash: '0xb82de527'
    type: UInt
    state: documented
  - name: CoinSilver
    hash: '0xb6e959dc'
    type: UInt
    state: documented
  - name: ContentsCountByFocusLeading
    hash: '0xa88537bf'
    type: UInt
    state: documented
  - name: ContentsCountByPickUp
    hash: '0x315da155'
    type: UInt
    state: documented
  - name: CurrentPlayDay
    hash: '0x90d48669'
    type: UInt
    state: documented
  - name: CurrentPlayMonth
    hash: '0x9c426820'
    type: UInt
    state: documented
  - name: CurrentPlayYear
    hash: '0x1f736dbd'
    type: UInt
    state: documented
  - name: CurrentSumDay
    hash: '0x9d3c40d2'
    type: UInt64
    state: documented
  - name: DailyLastNoInputPlayTime
    hash: '0xaa0a03b3'
    type: UInt64
    state: documented
  - name: DailyLastPlayTime
    hash: '0xfb9051f2'
    type: UInt64
    state: documented
  - name: EmotionBallGet
    hash: '0x602ee35d'
    type: UInt
    state: documented
  - name: EmotionBallSpent
    hash: '0xe515229c'
    type: UInt
    state: documented
  - name: EnterAtelierCount
    hash: '0x0ad95b4a'
    type: UInt
    state: documented
  - name: EnterEncyclopedia
    hash: '0x32bd0c79'
    type: UInt
    state: documented
  - name: EnterFacilityMarketMorning
    hash: '0x589806bb'
    type: UInt
    state: documented
  - name: EnterFacilityMarketNight
    hash: '0xcf659416'
    type: UInt
    state: documented
  - name: EnterFacilityMarketNoon
    hash: '0x7ada0701'
    type: UInt
    state: documented
  - name: EnterFacilityMarketSale
    hash: '0xd32b016d'
    type: UInt
    state: documented
  - name: EnterFacilityTower
    hash: '0xd79e9b5d'
    type: UInt
    state: documented
  - name: EnterPhotoStudioCount
    hash: '0x2b476487'
    type: UInt
    state: documented
  - name: FocusLeadingToMiiByPickUpCount
    hash: '0x0c61d79a'
    type: UInt
    state: documented
  - name: FocusLeadingToObjByPickUpCount
    hash: '0x07c6b3ea'
    type: UInt
    state: documented
  - name: FountainWishComeTrueCount
    hash: '0x50b090e3'
    type: UInt
    state: documented
  - name: InnerRoomChangeCount
    hash: '0xbf3432e6'
    type: UInt
    state: documented
  - name: InnerRoomLivingRoomChangeCount
    hash: '0xa2f27969'
    type: UInt
    state: documented
  - name: InnerRoomPrivateRoomChangeCount
    hash: '0xe6ba5127'
    type: UInt
    state: documented
  - name: LastUpdateTime
    hash: '0x82747a38'
    type: UInt64
    state: documented
  - name: MiiEventOnFocusLeadingMiiCount
    hash: '0xa2da5ee9'
    type: UInt
    state: documented
  - name: MiiEventOnFocusLeadingObjectCount
    hash: '0x26795746'
    type: UInt
    state: documented
  - name: MiiEventOnFollowingMiiCount
    hash: '0x7b26f300'
    type: UInt
    state: documented
  - name: Minigame
    hash: '0xbd65ba7a'
    type: UInt
    state: documented
  - name: MoneyGet
    hash: '0x93bb9006'
    type: UInt
    state: documented
  - name: MoneySpent
    hash: '0x640e7706'
    type: UInt
    state: documented
  - name: NoInputPlayTimeCameraDistanceLevel
    hash: '0xa8979ada'
    type: UInt64Array
    state: documented
  - name: OuterHouseChangeCount
    hash: '0x096493a2'
    type: UInt
    state: undocumented
  - name: PlayTimeCameraDistanceLevel
    hash: '0xeed6d279'
    type: UInt64Array
    state: documented
  - name: PresentClothCount
    hash: '0xea4d306f'
    type: UInt
    state: undocumented
  - name: PresentFoodCount
    hash: '0xd13e486c'
    type: UInt
    state: undocumented
  - name: ResolvedAccidentCount
    hash: '0x01d5e547'
    type: UInt
    state: documented
  - name: ResolvedTroubleCount
    hash: '0xec83ba91'
    type: UInt
    state: documented
---

import LeafMeta from '@components/LeafMeta.astro';
import Bounds from '@components/Bounds.astro';

The `DailyLog` group records per-day statistics: counters for what happened during the current in-game day, plus the calendar fields that date the block. Every counter starts at `0` and is cleared when the in-game date rolls over, so each value reflects only the current play day, not a lifetime total.

## Fields

### `AiCountByFocusLeading`

<LeafMeta name="AiCountByFocusLeading" leaves={frontmatter.leaves} />

Counts how many times today an encounter that began by a Mii approaching a target on its own resolved into a plain AI reaction.

### `AiCountByPickUp`

<LeafMeta name="AiCountByPickUp" leaves={frontmatter.leaves} />

Counts how many times today an encounter that began by picking a Mii up and dropping it on a target resolved into a plain AI reaction.

### `BuyMarketMorning`

<LeafMeta name="BuyMarketMorning" leaves={frontmatter.leaves} />

Counts purchases the player made at the market facility during its **Morning** session of the current day.

### `BuyMarketNight`

<LeafMeta name="BuyMarketNight" leaves={frontmatter.leaves} />

Counts purchases the player made at the market facility during its **Night** session of the current day.

### `BuyMarketNoon`

<LeafMeta name="BuyMarketNoon" leaves={frontmatter.leaves} />

Counts purchases the player made at the market facility during its **Noon** session of the current day.

### `BuyMarketSale`

<LeafMeta name="BuyMarketSale" leaves={frontmatter.leaves} />

Counts purchases the player made at the market facility while a **Special Sale** (the 80%-off event) was running during the current day.

### `CoinGold`

<LeafMeta name="CoinGold" leaves={frontmatter.leaves} />

Counts the **gold coins** goods the player received from Miis during the current day. A Mii hands over a gold coin as a level-up "payback" gift when its happiness reaches level 21.

### `CoinSilver`

<LeafMeta name="CoinSilver" leaves={frontmatter.leaves} />

Counts the **silver coins** goods the player acquired during the current day. A Mii hands over a silver coin as a level-up "payback" gift when its happiness reaches the highest level 20.

### `ContentsCountByFocusLeading`

<LeafMeta name="ContentsCountByFocusLeading" leaves={frontmatter.leaves} />

Counts how many times today an encounter that began by a Mii approaching a target on its own resolved into a short scripted content scene.

### `ContentsCountByPickUp`

<LeafMeta name="ContentsCountByPickUp" leaves={frontmatter.leaves} />

Counts how many times today an encounter that began by picking a Mii up and dropping it on a target resolved into a short scripted content scene.

### `CurrentPlayDay`

<LeafMeta name="CurrentPlayDay" leaves={frontmatter.leaves} />

The day-of-month component of the calendar date the `DailyLog` block currently belongs to. When the daily counters are rolled over, the game stamps the date from the console clock into the triplet `CurrentPlayYear` / `CurrentPlayMonth` / `CurrentPlayDay`. The game compares it against the current date to detect a day change and reset the daily counters.

<Bounds>
  <Fragment slot="ingame">`1`..`31`, the day of the month.</Fragment>
  <Fragment slot="accepted">any `UInt32`</Fragment>
</Bounds>

### `CurrentPlayMonth`

<LeafMeta name="CurrentPlayMonth" leaves={frontmatter.leaves} />

The calendar-month component of the date the `DailyLog` block currently belongs to. When the daily counters roll over, the game stamps the console clock's date into the triplet `CurrentPlayYear` / `CurrentPlayMonth` / `CurrentPlayDay`, and compares it against the current date to detect a day change and reset the daily counters.

<Bounds>
  <Fragment slot="ingame">
    `1`..`12`, the month (`1` = January … `12` = December). and `CurrentPlayYear`.
  </Fragment>
  <Fragment slot="accepted">any `UInt32`</Fragment>
</Bounds>

### `CurrentPlayYear`

<LeafMeta name="CurrentPlayYear" leaves={frontmatter.leaves} />

The calendar year of the date the `DailyLog` block currently belongs to. When the daily counters roll over, the game stamps the console clock's date into the triplet `CurrentPlayYear` / `CurrentPlayMonth` / `CurrentPlayDay`, and compares it against the current date to detect a day change and reset the daily counters.

<Bounds>
  <Fragment slot="ingame">the four-digit system year at the time of play (e.g. `2026`)</Fragment>
  <Fragment slot="accepted">any `UInt32`</Fragment>
</Bounds>

### `CurrentSumDay`

<LeafMeta name="CurrentSumDay" leaves={frontmatter.leaves} />

The `DailyLog`'s date as a single day number: days since `1970-01-01` for the same date as the `CurrentPlayYear` / `CurrentPlayMonth` / `CurrentPlayDay` triplet (e.g. `20581` = `2026-05-08`).

### `DailyLastNoInputPlayTime`

<LeafMeta name="DailyLastNoInputPlayTime" leaves={frontmatter.leaves} />

Snapshot of the lifetime idle-play counter [`Player.NoInputPlayTime`](../player/#noinputplaytime) (cumulative seconds of play with no player input) taken at the start of the current daily-log day.

### `DailyLastPlayTime`

<LeafMeta name="DailyLastPlayTime" leaves={frontmatter.leaves} />

Snapshot of the lifetime play-time counter [`Player.PlayTime`](../player/#playtime) (cumulative seconds of play) taken at the start of the current daily-log day.

### `EmotionBallGet`

<LeafMeta name="EmotionBallGet" leaves={frontmatter.leaves} />

Number of ([`Player.EmotionBall`](../player/#emotionball)) acquired on current `DailyLog`.

### `EmotionBallSpent`

<LeafMeta name="EmotionBallSpent" leaves={frontmatter.leaves} />

Number of ([`Player.EmotionBall`](../player/#emotionball)) spent on current `DailyLog`.

### `EnterAtelierCount`

<LeafMeta name="EnterAtelierCount" leaves={frontmatter.leaves} />

Count of how many times the player has entered the Atelier on current `DailyLog`.

### `EnterEncyclopedia`

<LeafMeta name="EnterEncyclopedia" leaves={frontmatter.leaves} />

Count of how many times the player has entered the Encyclopedia on current `DailyLog`.

### `EnterFacilityMarketMorning`

<LeafMeta name="EnterFacilityMarketMorning" leaves={frontmatter.leaves} />

Count of how many times the player has entered the marketplace in the morning on current `DailyLog`.

### `EnterFacilityMarketNight`

<LeafMeta name="EnterFacilityMarketNight" leaves={frontmatter.leaves} />

Count of how many times the player has entered the marketplace at night on current `DailyLog`.

### `EnterFacilityMarketNoon`

<LeafMeta name="EnterFacilityMarketNoon" leaves={frontmatter.leaves} />

Count of how many times the player has entered the marketplace at noon on current `DailyLog`.

### `EnterFacilityMarketSale`

<LeafMeta name="EnterFacilityMarketSale" leaves={frontmatter.leaves} />

Count of how many times the player has entered the marketplace during a sale on current `DailyLog`.

### `EnterFacilityTower`

<LeafMeta name="EnterFacilityTower" leaves={frontmatter.leaves} />

Count of how many times the player has entered the Tower on current `DailyLog`.

### `EnterPhotoStudioCount`

<LeafMeta name="EnterPhotoStudioCount" leaves={frontmatter.leaves} />

Count of how many times the player has entered the Photo Studio on current `DailyLog`.

### `FocusLeadingToMiiByPickUpCount`

<LeafMeta name="FocusLeadingToMiiByPickUpCount" leaves={frontmatter.leaves} />

Daily counter of how many times today the player **picked up a Mii and led it to another Mii**.

### `FocusLeadingToObjByPickUpCount`

<LeafMeta name="FocusLeadingToObjByPickUpCount" leaves={frontmatter.leaves} />

Daily counter of how many times today the player **picked up a Mii and led it toward an object or facility**.

### `FountainWishComeTrueCount`

<LeafMeta name="FountainWishComeTrueCount" leaves={frontmatter.leaves} />

Count of Wish used on current `DailyLog`.

### `InnerRoomChangeCount`

<LeafMeta name="InnerRoomChangeCount" leaves={frontmatter.leaves} />

Counts how many times the player **redecorated the interior** of a Mii's home this day. This is the sum of [`InnerRoomLivingRoomChangeCount`](#innerroomlivingroomchangecount) and [`InnerRoomPrivateRoomChangeCount`](#innerroomprivateroomchangecount).

### `InnerRoomLivingRoomChangeCount`

<LeafMeta name="InnerRoomLivingRoomChangeCount" leaves={frontmatter.leaves} />

Counts how many times the player **redecorated the living room** of a Mii's home this day.

### `InnerRoomPrivateRoomChangeCount`

<LeafMeta name="InnerRoomPrivateRoomChangeCount" leaves={frontmatter.leaves} />

Counts how many times the player **redecorated** a Mii's **bedroom** this day.

### `LastUpdateTime`

<LeafMeta name="LastUpdateTime" leaves={frontmatter.leaves} />

The UNIX timestamp of the last daily-log rollover.

### `MiiEventOnFocusLeadingMiiCount`

<LeafMeta name="MiiEventOnFocusLeadingMiiCount" leaves={frontmatter.leaves} />

Counts the Mii events that played out today after the player led a Mii toward another Mii.

### `MiiEventOnFocusLeadingObjectCount`

<LeafMeta name="MiiEventOnFocusLeadingObjectCount" leaves={frontmatter.leaves} />

Counts the Mii events that played out today after the player led a Mii toward an object or facility.

### `MiiEventOnFollowingMiiCount`

<LeafMeta name="MiiEventOnFollowingMiiCount" leaves={frontmatter.leaves} />

Counts the Mii events that played out today, triggered while one Mii was following another on its own.

### `Minigame`

<LeafMeta name="Minigame" leaves={frontmatter.leaves} />

Counts the **minigames** played during the current day.

### `MoneyGet`

<LeafMeta name="MoneyGet" leaves={frontmatter.leaves} />

Amount of ([`Player.Money`](../player/#money)) the player acquired on current `DailyLog`.

### `MoneySpent`

<LeafMeta name="MoneySpent" leaves={frontmatter.leaves} />

Amount of ([`Player.Money`](../player/#money)) the player spent on current `DailyLog`.

### `NoInputPlayTimeCameraDistanceLevel`

<LeafMeta name="NoInputPlayTimeCameraDistanceLevel" leaves={frontmatter.leaves} />

A fixed three-element array of cumulative seconds the player held the room/island **camera** at each of its three distance levels this day **while giving no input**, one count per slot from `0` (closest) to `2` (farthest).

### `OuterHouseChangeCount`

<LeafMeta name="OuterHouseChangeCount" leaves={frontmatter.leaves} />

{/* Describe what the game uses this field for. */}

### `PlayTimeCameraDistanceLevel`

<LeafMeta name="PlayTimeCameraDistanceLevel" leaves={frontmatter.leaves} />

A fixed three-element array of cumulative seconds the player held the room/island **camera** at each of its three distance levels this day, one count per slot from `0` (closest) to `2` (farthest).

### `PresentClothCount`

<LeafMeta name="PresentClothCount" leaves={frontmatter.leaves} />

{/* Describe what the game uses this field for. */}

### `PresentFoodCount`

<LeafMeta name="PresentFoodCount" leaves={frontmatter.leaves} />

{/* Describe what the game uses this field for. */}

### `ResolvedAccidentCount`

<LeafMeta name="ResolvedAccidentCount" leaves={frontmatter.leaves} />

Number of accidents resolved on current `DailyLog`.

### `ResolvedTroubleCount`

<LeafMeta name="ResolvedTroubleCount" leaves={frontmatter.leaves} />

Number of troubles resolved on current `DailyLog`.
